package gui;

import main.Subject;

/**
 * This interface represent the Observer in an observer design pattern
 * @author all
 */
public interface Observer {
    public void update(Subject subject);
}
