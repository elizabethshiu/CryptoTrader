package main;

/**
 * Main class that launches the app, starting with the login UI
 * @author all
 */
 
public class main{
    public static void main(String[] args){
        LoginUI login = LoginUI.getInstance(); //launch login UI window
    }
}